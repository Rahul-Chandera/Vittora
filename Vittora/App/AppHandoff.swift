import Foundation

/// Handoff / Continuity activity for restoring the active tab across devices.
enum AppHandoff {
    static let activityType = "com.enerjiktech.vittora.main"
    static let tabKey = "selectedTab"
}
