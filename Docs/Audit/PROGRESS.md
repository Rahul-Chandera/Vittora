# Audit Remediation Progress

Running log — one row per task. See `EXECUTION_PLAN.md` for task definitions and `CURSOR_HANDOFF.md` → *Branching, pushing & review* for the working agreement.

> Branching: every task branches off `refactoring` (or its real parent when stacked); self-verified tasks are merged into `refactoring` to advance the base. Local commits only — no push/PR. The reviewer reviews read-only via refs (`git diff <base>...<branch>`).

| Task | Branch | Base | Status | Tests added | Verify result | Notes/assumptions |
|------|--------|------|--------|-------------|---------------|-------------------|
| A10 | fix/A10-delete-cascade | refactoring (post-A9) | Merged into refactoring | LedgerWriteStoreTests: performDeleteCategoryNullifiesDependents, performDeleteRecurringRuleNullifiesTransactions; CategoryUseCaseTests.testDeleteNullifiesTransactionCategory + testThrowsWhenDeletingDefaultCategory; RecurringUseCaseTests.deleteRecurringRuleNullifiesLinkedTransactions | build-ios✅ build-macos✅ (LedgerWriteStoreTests+CategoryUseCaseTests+RecurringUseCaseTests)✅ | DATAINTEGRITY-6 / ARCHITECTURE-05. Chose **nullify** (not block) for category delete — matches payee pattern and keeps user data. New `LedgerWriting.performDeleteCategory` / `performDeleteRecurringRule` in `LedgerWriteStore` (one save): nullifies `categoryID` on transactions/budgets/recurring templates/group expenses + `parentID` on child categories, then deletes category; nullifies `recurringRuleID` on generated txs, then deletes rule. `DeleteCategoryUseCase` / `DeleteRecurringRuleUseCase` require `any LedgerWriting` (no repo-only fallback). Views wire `dependencies.ledgerWriteStore`. Default-category guard in use case + store (defense in depth). Dependent fetches unbounded (correct — must nullify all). No balance effects. |
| A12 | fix/A12-split-rounding | refactoring (post-A11) | Merged into refactoring | SplitGroupUseCaseTests: equalSplitsSumExactly, percentageSplitsSumExactly, percentageSumValidation, sharesSplitAvoidsNegativeLastShare, sharesRejectsZeroWeights, transfersNetToZero | build-ios✅ build-macos✅ SplitGroupUseCaseTests✅ | DATAINTEGRITY-9. New `SplitRounding` helper (shared epsilon/tolerance, allocate-with-remainder + negative clamp). Percentage sum validated to 100±0.01; all proportional methods use last-member remainder. `SimplifyDebtsUseCase` rounds nets/transfers to cents with shared epsilon; `AddGroupExpenseViewModel.recalculate` delegates to use case. Constants use exact `Decimal(sign:exponent:significand:)` (no force-unwrap). |
| A13 | fix/A13-india-surcharge-relief | refactoring (post-A12) | Merged into refactoring | TaxCalculatorRegressionTests/IndiaFY2025SurchargeMarginalRelief (₹50L/₹51L, ₹1Cr/₹1.01Cr, ₹2Cr/₹2.01Cr, ₹5Cr/₹5.01Cr boundary vectors + equity STCG 15% surcharge cap) | build-ios✅ build-macos✅ test-tax✅ | DATAINTEGRITY-8. Surcharge marginal relief caps **pre-cess** (income tax + surcharge), then 4% cess applied — statutory ordering (₹51L official ≈ ₹12,03,800). `rawSurcharge` splits ordinary vs special-rate with 15% cap on equity LTCG/STCG. `TaxCoreAmounts` holds deduction/bracket fields once (no duplicate slab pass). Review fix: removed post-cess cap + circular test vectors. |
| B1 | fix/B1-app-lock-timeout | refactoring (post-A13) | Merged into refactoring | AppLockServiceTests: AppLockPolicy.shouldLock boundaries + recordBackgrounded; SettingsViewModelTests: appLockTimeout default/persist | build-ios✅ build-macos✅ AppLockServiceTests+SettingsViewModelTests✅ | SECURITY-1. Removed dead inactivity timer; `recordBackgrounded(at:)` on `.background`; re-lock on `.active` via pure `AppLockPolicy.shouldLock`. Settings: Immediately / 1m / 5m (`AppLockTimeout`, default 5m). Privacy shield unchanged; no immediate lock on every background. Fixed pre-existing flaky cooldown escalation test (`testing_clearCooldown` DEBUG hook). |
| B2 | fix/B2-app-lock-fail-closed | refactoring (post-B1) | Merged into refactoring | AppLockServiceTests: AppLockUnlockGate missing-service fail-closed + message | build-ios✅ build-macos✅ AppLockServiceTests✅ | SECURITY-5. Nil `appLockService` no longer sets `isLocked = false`. `AppLockUnlockGate` keeps session locked; `AppLockView` shows localized error + Retry (hides passcode path when service absent). |
| B3 | fix/B3-biometric-destructive-gate | refactoring (post-B2) | Merged into refactoring | SensitiveActionAuthenticatorTests; SettingsViewModelTests: disableAppLock cancel/success | build-ios✅ build-macos✅ SensitiveActionAuthenticatorTests+SettingsViewModelTests✅ | SECURITY-3. `SensitiveActionAuthenticator` gates disable App Lock + factory reset (Settings delete-all + DataManagement) via `BiometricService.authenticate()`; abort on cancel/failure. Custom App Lock toggle binding for disable path. |
| B4 | fix/B4-passcode-fallback-toggle | refactoring (post-B3) | Merged into refactoring | AppLockServiceTests: passcode fallback policy + forwards flag + biometrics-only skips passcode; BiometricServiceTests updated | build-ios✅ build-macos✅ AppLockServiceTests+BiometricServiceTests✅ | SECURITY-2. `allowPasscodeFallback` setting now enforced: `AppLockView` hides "Use Passcode" + biometrics-only hint when off; `AppLockService.unlock(allowPasscodeFallback:)` forwards to `BiometricService.authenticate`; empty fallback title suppresses system passcode button; no auto-fallback on lockout/unavailable when disabled. Destructive actions still allow passcode via `SensitiveActionAuthenticator`. |
| B5 | fix/B5-cooldown-keychain-persist | refactoring (post-B4) | Merged into refactoring | AppLockServiceTests: cooldown survives re-init, expired re-arm, keychain round-trip; DataManagementServiceTests: factory reset clears cooldown key | build-ios✅ build-macos✅ AppLockServiceTests+DataManagementServiceTests✅ | SECURITY-4. `AppLockCooldownStore` persists `consecutiveFailures` + `cooldownExpiresAt` to Keychain (`vittora.appLockCooldown`); restored on init with expired cooldown cleared; factory reset deletes key. |
| B6 | fix/B6-encryption-single-flight | refactoring (post-B5) | Merged into refactoring | EncryptionServiceTests: parallel encrypt creates one key (serialized suite, legacy test path) | build-ios✅ build-macos✅ EncryptionServiceTests✅ | SECURITY-6. Single-flight `keyCreationTask` + in-memory `cachedKey` coalesce concurrent get-or-create; idempotent re-check in `generateLegacyKey`; DEBUG test hook forces legacy path off-device. |
| B7 | fix/B7-security-error-mapping | refactoring (post-B6) | Merged into refactoring | SecurityErrorMapperTests: OSStatus/CFError/underlying not in user message | build-ios✅ build-macos✅ SecurityErrorMapperTests+EncryptionServiceTests✅ | SECURITY-12. `SecurityErrorMapper` maps Keychain OSStatus + SE CFError to localized non-diagnostic messages; raw platform details logged via `PerformanceLogger.Security.platformFailure` only. |
| C1 | fix/C1-notification-service | refactoring (post-B7) | Merged into refactoring | NotificationServiceTests: categories, auth, schedule/cancel, deep link | build-ios✅ build-macos✅ NotificationServiceTests✅ | FUNCTIONAL-1. `NotificationService` wraps `UNUserNotificationCenter` via `UserNotificationCenterProtocol` + mock; schedule/cancel, category registration, deep-link tap → `AppState.openFromNotification`. Registered in `DependencyContainer`; configured at startup in `VittoraApp`. |
| C2 | fix/C2-onboarding-notification-priming | refactoring (post-C1) | Merged into refactoring | OnboardingViewModelTests: notifications step in flow, skippable, opt-in/skip persistence | build-ios✅ build-macos✅ OnboardingViewModelTests✅ | FUNCTIONAL-21. New onboarding step after account setup (before done): value-first priming + optional toggle; persists `vittora.notificationsEnabled` intent only — no `requestAuthorization()` (deferred to C6 Settings enable). |
| C3 | fix/C3-budget-threshold-alerts | refactoring (post-C2) | Merged into refactoring | BudgetUseCaseTests: newlyCrossedThresholds + EvaluateBudgetThresholdAlertsUseCase (50/75/90/100 once, dedupe, disabled skip) | build-ios✅ build-macos✅ BudgetUseCaseTests✅ | FUNCTIONAL-2. `EvaluateBudgetThresholdAlertsUseCase` fetches active budgets, detects newly crossed 50/75/90/100 thresholds per budget period, dedupes via `UserDefaultsBudgetThresholdAlertStore`, schedules via C1 `NotificationService`. Wired after transaction save (`TransactionFormView`, `QuickEntryView`) + `DependencyContainer.refreshBudgetThresholdAlerts()`. Respects `vittora.notificationsEnabled` + `vittora.notifyBudgetAlerts`. |
| C4 | fix/C4-credit-card-due-reminders | refactoring (post-C3) | Merged into refactoring | CreditCardDueReminderTests + AccountMapperTests + ModelContainerConfigTests V6 round-trip + CreateAccountUseCaseTests billing days | build-ios✅ build-macos✅ CreditCardDueReminderTests✅ | FUNCTIONAL-4. Optional `statementDayOfMonth` + `dueDayOfMonth` on credit cards (**Schema V6**). Account form billing-cycle pickers; `ScheduleCreditCardDueRemindersUseCase` schedules pre-due (3 days) `billDue` notifications via C1; refresh on account save + app startup. Deep link `.accountDetail`. Respects `vittora.notificationsEnabled` + `vittora.notifyBillsDue`. |
| C5 | fix/C5-recurring-debt-reminders | refactoring (post-C4) | Merged into refactoring | RecurringDebtReminderTests: PaymentReminderDateCalculator, recurring pre-notif, self-debt schedule/cancel, contact draft | build-ios✅ build-macos✅ RecurringDebtReminderTests✅ | FUNCTIONAL-16 / M1.4.3. `ScheduleRecurringPreNotificationsUseCase` (1 day before `nextDate`, `vittora.notifyRecurring`); `ScheduleSelfDebtDueRemindersUseCase` for `.borrowed` debts with due dates (`vittora.notifyBillsDue`); `DebtContactReminderDraft` + ShareLink **Remind** on lent entries (user-initiated only). Refresh after debt/recurring save, settle, pause/delete, and post-launch recurring generation. |
| C6 | fix/C6-notification-settings-wiring | refactoring (post-C5) | Merged into refactoring | ApplyNotificationPreferencesUseCaseTests: OS prompt, disable cancel, sub-toggle refresh | build-ios✅ build-macos✅ NotificationPreferencesTests✅ | UX-4. Master toggle requests `NotificationService.requestAuthorization()`; denied → revert intent + alert. Disable → `cancelAllPending()`. Sub-toggles call `refreshAllNotificationSchedules()` (budget/credit card/recurring/debt). `DependencyContainer.refreshAllNotificationSchedules()` helper. |
| D1–D8 | fix/D-compliance-release-config | refactoring (post-C6) | Merged into refactoring | plutil manifest/entitlements; build-ios✅ build-macos✅ | COMPLIANCE Epic D. D1 UserDefaults CA92.1 in privacy manifest. D2 removed push (`aps-environment`, `remote-notification`); kept `fetch` for BGAppRefresh. D3 `Vittora-macOS.entitlements` sandbox+camera/contacts/photos/network+CloudKit. D4 dropped visionOS from platforms/device family. D5 metadata scoped to shipped features. D6 fixed SYSTEM_MAP path + new `Docs/Compliance/Privacy_Compliance_Checklist.md` + RELEASE_CHECKLIST items. D7 removed `NSPhotoLibraryUsageDescription`. D8 documented `ITSAppUsesNonExemptEncryption=false` rationale. |
| A11 | fix/A11-debt-settlement-links | refactoring (post-A10) | Merged into refactoring | DebtUseCaseTests.twoPartialSettlementsRetained; DebtMapperTests.testToEntityMergesLegacyLinkedTransactionID; ModelContainerConfigTests.onDiskStoreRoundTripsLinkedTransactionIDs + migrationPlanShape V5 | build-ios✅ build-macos✅ DebtUseCaseTests+DebtMapperTests+LedgerWriteStoreTests+ModelContainerConfigTests✅ | DATAINTEGRITY-7. Replaced single `linkedTransactionID` with append-only `linkedTransactionIDs: [UUID]` on `DebtEntry`; persisted as `SDDebt.linkedTransactionIDsJSON` (CloudKit-safe String, computed accessor — same pattern as `SDSplitGroup.memberIDsJSON`). **Schema V5** (additive JSON column + legacy scalar kept). `performSettle`/`appendLinkedTransaction` promotes legacy link then appends; `DebtMapper` merges legacy on read. |
| A1 | fix/A1-ledger-write-store | refactoring | Merged into refactoring | LedgerWriteStoreTests (one-save, rollback, recovery, seeded-stub) | build-ios✅ build-macos✅ test-data✅ LedgerWriteStoreTests✅ | `@ModelActor LedgerWriteStore` + `commit(_:)` Unit-of-Work (one `save()`, `rollback()` on failure) + `saveCount` test hook. Operation entry points (`performTransfer/Add/Settle/Delete`) seeded as stubs throwing `LedgerWriteError.notImplemented`; bodies land in A3/A4/A6. Registered in `DependencyContainer`. |
| A2 | fix/A2-transfer-pair-id | refactoring | Merged into refactoring | ModelContainerConfigTests.onDiskStoreRoundTripsTransferPairID + migrationPlanV1toV2Shape; TransactionMapperTests transferPairID assertions | build-ios✅ build-macos✅ test-data✅ ModelContainerConfigTests✅ | Optional `transferPairID: UUID?` on `TransactionEntity`/`SDTransaction` + both mapper directions + repo `create`. `VittoraSchemaV2` (additive) + `.lightweight` V1→V2 stage; `allModels`→V2. Review fix: round-trip test renamed (it's persistence, not a V1→V2 migration). I4 follow-up logged: `VittoraSchemaV2.models` aliases V1.models, so a faithful V1→V2 test needs a frozen `SDTransactionV1` snapshot. Out of scope (NOT fixed): `TransactionMapperTests.testToEntityMapsDefaults` hardcodes `currencyCode=="USD"`, fails in non-USD locale (this machine=INR); pre-existing. |
| A6 | fix/A6-atomic-add-settle | refactoring (post-A1+A2) | Merged into refactoring | LedgerWriteStoreTests (performAdd/performSettle persistence + 3 rollback cases, real container); AddTransactionUseCaseTests + SettleDebtUseCaseTests rewritten on real container | build-ios✅ build-macos✅ LedgerWriteStoreTests✅ TransactionUseCaseTests✅ DebtUseCaseTests✅ TransactionFormViewModelTests✅ | DATAINTEGRITY-2: Add/Settle now write atomically. New `protocol LedgerWriting` (Sendable); `LedgerWriteStore` conforms; `performAdd`/`performSettle` do read-modify-write inside `commit` (one `save()`, rollback on any failure). `AddTransactionUseCase`/`SettleDebtUseCase` take a REQUIRED `any LedgerWriting` (no repository fallback); use cases stay thin (validate → perform). Views/seeder unwrap `dependencies.ledgerWriteStore` or skip (I1 makes it non-optional). **Behavior change (for review):** settling against a non-existent account now throws atomically instead of silently creating an orphan transaction. View-model tests use a non-atomic `MockLedgerWriting`; atomicity is proven only against the real store. `performTransfer`/`performDelete` remain A3/A4 stubs. |
| A5 | fix/A5-update-account-reversal | refactoring (post-A1) | Merged into refactoring | TransactionUseCaseTests.editChangingAccountUpdatesBothBalances + editChangingAccountMovesIncome | build-ios✅ build-macos✅ TransactionUseCaseTests✅ | DATAINTEGRITY-3: `UpdateTransactionUseCase` now reverses the original effect on the ORIGINAL account and applies the new effect on the NEW account (single net update when unchanged; both accounts when changed) via a `balanceEffect(type:amount:)` helper; removed the `?? UUID()` hack. Scope note: single-`save()` atomic wiring of update through `LedgerWriteStore` is left to A4 (which reworks Update for `.transfer`); A5 fixes the account-targeting correctness (DI-3 acceptance), fully unit-tested with mocks (mock failure-injection for rollback is L2). |
| A3 | fix/A3-transfer-funds | refactoring (rebased post-A6) | Merged into refactoring | AccountUseCaseTests.TransferFundsUseCase (paired debit/credit legs+one save, transferIsBalanceNeutral, transferRollsBackOnPartialFailure, rejects-same-account) on real in-memory container; ModelContainerConfigTests.onDiskStoreRoundTripsTransferDirection + migrationPlanShape (V3); TransactionUseCaseTests.testRejectsTransferThroughGenericAdd | build-ios✅ build-macos✅ combined-suite✅ (LedgerWriteStoreTests + TransactionUseCaseTests + DebtUseCaseTests + TransactionFormViewModelTests + AccountUseCaseTests + ModelContainerConfigTests) | DATAINTEGRITY-1/2. Option A transfer model: additive optional `TransferDirection` (.debit/.credit) on `TransactionEntity`/`SDTransaction` (+mapper/repo) → **Schema V3** (`transferDirectionRawValue`) + `.lightweight` V2→V3 (A3 merged first → claims V3; A7 rebases to V4). `LedgerWriteStore.performTransfer` atomic: two legs share one `transferPairID` (source=.debit, dest=.credit), both balances applied, one save; rollback on missing account. `TransferFundsUseCase` requires the concrete `LedgerWriteStore`; `TransferFormView` unwraps `dependencies.ledgerWriteStore`. **Integrated onto A6 (rebase, per review):** canonical `TransactionEntity.signedBalanceEffect` is now the SINGLE source of truth — adopted by `LedgerWriteStore.performAdd`/`performSettle`, `UpdateTransactionUseCase`, and `MockLedgerWriting`; the store-local and mock `balanceEffect` copies are DELETED. Account-not-found is unified to `LedgerWriteError.accountNotFound(UUID)` across ALL store ops (transfer/add/settle); debt-not-found stays `VittoraError.notFound`. `performAdd` now REJECTS `.transfer` (`LedgerWriteError.transferNotSupported`) so transfers can only flow through `performTransfer`. |
| A7 | fix/A7-balance-reconciliation | refactoring (rebased post-A3) | Merged into refactoring | ReconcileAccountBalanceUseCaseTests (detect+repair idempotent, reconciled-not-flagged, legacy-nil-opening skipped, legacy-nil-direction-transfer skipped, **direction-carrying-transfer reconciled**, manual-edit no-false-drift via UpdateAccountUseCase, uncapped >500); ModelContainerConfigTests.onDiskStoreRoundTripsOpeningBalance + migrationPlanShape (V4) | build-ios✅ build-macos✅ test-sync✅ ModelContainerConfigTests✅ | DATAINTEGRITY-12. Additive optional `openingBalance: Decimal?` on `AccountEntity`/`SDAccount` + mapper + repo `create`; **Schema V4** (`openingBalance`) + `.lightweight` V3→V4 stage (rebased off A3's V3; `allModels`→V4). New accounts seed `openingBalance = balance`; manual balance edits in `UpdateAccountUseCase` re-baseline opening by the delta (so repair won't revert them). `ReconcileAccountBalanceUseCase.detectDrift()/repair()` replays effects over opening. **Integrated onto A3 (rebase, per review):** uses canonical `TransactionEntity.signedBalanceEffect` (A7's local `balanceEffect` DELETED); now **includes direction-carrying transfer legs** (each leg's `signedBalanceEffect` on its `accountID`) — only LEGACY `nil`-direction transfer legs are skipped (account excluded). Legacy `nil`-opening accounts treated as reconciled (implied opening derived on read, never persisted — CloudKit-safe). Uncapped reconciliation fetch on `TransactionRepository`. "Repair Account Balances" action wired into `SyncDetailView`. Closes A7's transfer TODO. |
| A9 | fix/A9-locale-money-parser | refactoring (post-A4 merge) | Merged into refactoring |
| A4 | fix/A4-transfer-delete-update | refactoring (post-A8 merge) | Merged into refactoring | LedgerWriteStoreTests: performDeleteReversesNonTransfer, performDeleteReversesBothTransferLegs, performDeleteThrowsWhenMissing, performUpdateNetsSameAccount, performUpdateMovesAccount, performUpdateRejectsTransfer, performUpdateTransferReamountsBothLegs, performUpdateTransferMovesAccounts; TransactionUseCaseTests.deleteTransferReversesBothLegs + updateRejectsTransferLeg; AccountUseCaseTests.UpdateTransferUseCaseTests (edit amount; reject same-account) | build-ios✅ build-macos✅ (LedgerWriteStoreTests+TransactionUseCaseTests+TransactionFormViewModelTests+AccountUseCaseTests+DebtUseCaseTests)✅ | DATAINTEGRITY-1. New atomic store ops `performDelete`/`performUpdate`/`performUpdateTransfer` (one save, rollback). `performDelete` reverses BOTH legs of an A3 transfer via `transferPairID` + `signedBalanceEffect` and deletes both rows. `Delete`+`Update` use cases now route through `any LedgerWriting` (atomic); bulk delete moved onto `DeleteTransactionUseCase.executeBulk` and the old non-atomic `BulkOperationsUseCase.bulkDelete` removed. **Option B guard:** generic `UpdateTransactionUseCase` rejects transfer-leg edits (+ defense-in-depth in `performUpdate`); transfer edits use the dedicated `UpdateTransferUseCase` → `performUpdateTransfer`. **Legacy nil-`transferPairID` (best-effort, documented):** signedBalanceEffect 0 → delete removes only the selected row, no balance change, partner left untouched; `performUpdateTransfer` rejects legacy nil-direction pairs. Removed the now-unused `LedgerWriteError.notImplemented`. **Follow-up F-A4a (non-blocking):** no transfer-edit UI entry yet — generic form safely rejects; wiring `UpdateTransferUseCase` to a dedicated screen is a separate UI task. |
| A8 | fix/A8-recurring-idempotency | refactoring (rebased post-A7) | Merged into refactoring | RecurringUseCaseTests: recurringCoordinatorCoalescesConcurrentRuns, generateRecurringTransactionsCatchesUpStaleRule, generateRecurringTransactionsKeepsMonthEndAnchor, …MatchesExistingByCalendarDay, …SelfHealsRuleUpdateFailure (+ existing suite updated to new API) | build-ios✅ build-macos✅ test-recurring✅ | DATAINTEGRITY-4 + DATAINTEGRITY-10. New `RecurringGenerationCoordinator` actor serializes/coalesces generation so app-launch + BGTask can't overlap (single shared instance built in `DependencyContainer`; both `VittoraApp` launch path and `BackgroundTaskScheduler` funnel through `generate()`). Generation routes create+balance through the merged A6 `LedgerWriting.performAdd` (atomic, one save). Idempotency keyed by `(recurringRuleID, calendar day)` — matches by `startOfDay`, not exact `Date`; a failure between the atomic add and the separate rule-pointer update self-heals on the next run (skip + advance, no double-charge). Catch-up loops every missed occurrence while `nextDate <= now` (and `<= endDate`). Month-end anchor preserves last-day-of-month (Jan-31→Feb-28→Mar-31), clamping only shorter months. Chose serialization over a unique constraint to stay schema-independent (no V3/V4 collision with A3/A7). `calendar`+`nowProvider` injected for deterministic tests. **Known limitation (for review):** a non-last-day day >28 (e.g. the 30th) clamped in Feb then sticks to month-end; a faithful fix needs an additive `anchorDay` schema field, deferred to avoid schema-version collision. Also tweaked A6's `MockLedgerWriting.debtRepository` to default `nil`. **Approved follow-ups (non-blocking, tracked as F-A8a/F-A8b):** (a) anchor monthly to the rule's ORIGINAL day-of-month so day-29/30 rules don't promote to month-ends after February (+ a Jan30→Feb28→Mar30 test); (b) recurring is currently expense-only (no template type) — recurring income/salary unsupported; track separately. |
